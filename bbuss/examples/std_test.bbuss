#include "../lib/std.bbuss"
#include "../lib/libc.bbuss"

cache func fib(int n) int {
    if(n le 1) {
        return n;
    }

    int result = (fib(n - 1) + fib(n - 2));
    return result;
}


func main() int {

    /*
    for(int i = 0; i lt 50; i = i + 1;) {
        str key := Itoa(i);

        SetM(key, i*3);
    }


    for(int i = 0; i lt 50; i = i + 1;) {
        str key := Itoa(i);

        int val := GetM(key);
        printf("value: %d", val);
    }
    */
    int result = safe fib(3);
    printf("result = %s", Itoa(result));


    return 0;
}
