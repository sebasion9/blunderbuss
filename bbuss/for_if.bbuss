func twenty() int {
    return 20;
}

func main() int {

    int hundred = 100;
    for(int i = 1; i le 20; i = i + 1) {
        int p1;
        //int prod = hundred / i;
        int prod = i;
        p1 = prod;

        if(prod ne 10) {
            printf("if:lt 10 = %d    ", p1);
        } else if (prod lt 15) {
            printf("if:lt 15 = %d    ", p1);
        } else {
            printf("if:prod = %d    ", p1);
        }
    }

    return 0;
}
